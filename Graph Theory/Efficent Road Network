#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Created on Thu Dec  8 15:30:51 2016

@author: brendontucker
"""

n = 6
roads = [[3, 0], [0, 4], [5, 0], [2, 1],
          [1, 4], [2, 3], [5, 2]]

boo1 = False
list1 = list()
length = n - 1
counter1 = 0
while counter1 <= length:
    list1.append(counter1)
    counter1 = counter1 + 1

pos1 = 0
pos2 = 1
counter1 = 0
counter2 = 0
connectValue = None
#connectList = list()
connectList = [3, 4, 5]
connectList2 = list()
counter4 = 0
counter5 = 0
length2 = len(connectList) - 1
    
while counter4 <= length2: 
    counter3 = 0
    connectValue2 = None
    while counter3 <= length:
        if roads[counter3][pos1] == connectList[counter4]: 
            connectValue2 = roads[counter3][pos2]
            if len(connectList2) > 0:
                for x in connectList2:
                    if connectValue2 == x:
                        counter3 = counter3 + 1
                    else:
                        connectList2.append(connectValue2)
                        counter3 = counter3 + 1
            else:
                connectList2.append(connectValue2)
        elif roads[counter3][pos2] == connectList[counter4]:
            connectValue2 = roads[counter3][pos1]
            if len(connectList2) > 0:
                while counter5 <= (len(connectList2) - 1):
                    if connectValue2 == connectList2[counter5]:
                        counter5 = counter5 + 1
                    else:
                        connectList2.append(connectValue2)
            else:
                connectList2.append(connectValue2)
        else:
            counter3 = counter3 + 1
    
    counter4 = counter4 + 1

'''
while counter2 <= 0: #should be this [while counter2 <= length:]
    while counter1 <= length:
        if roads[counter1][pos1] == counter2: 
            connectValue = roads[counter1][pos2]
            connectList.append(connectValue)
            counter1 = counter1 + 1
        elif roads[counter1][pos2] == counter2:
            connectValue = roads[counter1][pos1]
            connectList.append(connectValue)
            counter1 = counter1 + 1
        else:
            counter1 = counter1 + 1
    
    counter4 = 0
    length2 = len(connectList) - 1
    
    while counter4 <= length2: 
        counter3 = 0
        connectList2 = list()
        connectValue2 = None
        while counter3 <= length:
            if roads[counter3][pos1] == connectList[counter4]: 
                connectValue2 = roads[counter3][pos2]
                connectList2.append(connectValue2)
                counter3 = counter3 + 1
            elif roads[counter3][pos2] == connectList[counter4]:
                connectValue2 = roads[counter3][pos1]
                connectList2.append(connectValue2)
                counter3 = counter3 + 1
            else:
                counter3 = counter3 + 1
        
        counter4 = counter4 + 1
        #only want these added once so have to check for originality 
        #new connectList
        #if connectList2 + connectList 1 .sort =! list1
        #boo1 = False, break
        #else, clear both lists 
        #then add 1 to counter2 and do it all again
    counter2 = counter2 + 1
    #counter1 = 0 #could index connectList values and then do a boolean 
    #need something to store the list here
'''  